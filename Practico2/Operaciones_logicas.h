//Operaciones_logicas.h
#ifndef Operaciones_logicas
#define Operaciones_logicas

void swap(size_t var1, size_t var2);
//Swaps two given variables. The caller should check that the type of the
//variables are the same beforehand.

#endif